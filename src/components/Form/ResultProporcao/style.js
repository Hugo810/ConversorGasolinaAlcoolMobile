import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    textTitle:{
        color: "#ff0043",
        fontSize: 24,
        fontWeight: "bold"
    }
});

export default styles